{
  "name": "bring-gift-card-app-container",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build:web",
    "check:web": "npm --prefix frontend run check:web",
    "typecheck": "npm --prefix frontend run typecheck"
  },
  "engines": {
    "node": "22.x"
  }
}
